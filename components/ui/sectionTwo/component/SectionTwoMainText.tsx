import React from 'react';
import '../../../../style/Section2.css';

const SectionMainText = ({ text }: { text: string }) => {
  return <h1 className="SectionTwoMainText">{text}</h1>;
};

export default SectionMainText;
